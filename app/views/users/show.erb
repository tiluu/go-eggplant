<h1><%= @user.name %>'s Profile</h1>

<p><h2>Trips</h2></p>

<p><%= link_to 'new trip', new_user_trip_path(@user.id) %> 

<p>
    <% @trips.each do |trip| %>
        <%= link_to "#{trip.name}", trip_url_path(@user.id, trip.url) %><br>
    <% end %>
